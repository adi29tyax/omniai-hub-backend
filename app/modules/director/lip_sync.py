def generate_lip_sync(text: str, audio_url: str):
    # Placeholder for lip sync generation (Rhubarb, etc.)
    # Returns a list of mouth shapes and timestamps
    return {
        "metadata": {
            "times": [0.0, 0.5, 1.0, 1.5],
            "mouth_shapes": ["A", "B", "C", "X"]
        }
    }
